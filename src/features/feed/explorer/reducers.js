// ACCOUNT REDUCERS
import * as reduce from './types';

const initialState = {
};

const explorer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};


export default explorer;
