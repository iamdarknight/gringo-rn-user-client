// ACCOUNT REDUCERS
import * as reduce from './types';

const initialState = {
};

const flags = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};


export default flags;
